# ansible/playbooks/check_containers.yml
---
- name: Check Podman containers
  hosts: control[0]
  gather_facts: false
  tasks:
    - name: List running containers
      command: podman ps
      register: containers_json
    - name: Show container list
      debug:
        var: containers_json.stdout




#        var:
#      register: containers_json
#        var: containers_json.stdout

#      debug:
#        var: containers_json.stdout
#      register: containers_json

#  become: yes
#    - name: Show container list
#      debug:
#        var: containers_json.stdout

#    - name: Get container information
#      community.general.podman_container_info:
#        name: all
#      register: container_info



#---
#- name: Get container information using Docker API
#  hosts: all
#  become: yes
#  tasks:
#    - name: Collect container facts
#      community.general.docker_container_info:
#        name: all
#      register: container_info
#
#    - name: Display container status
#      debug:
#        var: container_info.containers

#---
#- name: Get Podman container information using containers.podman collection
#  hosts: all
#  become: yes
#  tasks:
#    - name: Get list of all containers
#      containers.podman.podman_container:
#        state: present
#        list: yes
#      register: containers_list
#
#    - name: Inspect each container
#      containers.podman.podman_container:
#        name: "{{ item.Name }}"
#        state: inspected
#      loop: "{{ containers_list.containers }}"
#      loop_control:
#        label: "{{ item.Name }}"
#      register: containers_inspected
#
#    - name: Display container information
#      debug:
#        var: containers_inspected.results

#- name: Get Podman container information
#  hosts: all
#  become: yes
#  tasks:
#    - name: Get containers JSON
#      command: podman ps -a
##        --format json
##      register: containers_json