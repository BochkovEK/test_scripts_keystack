# ansible/playbooks/check_containers.yml
#---
#- name: Check Podman containers
#  hosts: all
#  become: yes
#  tasks:
#    - name: List running containers
#      command: podman ps
#      register: container_list
#
#    - name: Show container list
#      debug:
#        var: container_list.stdout

---
- name: Get container information using Docker API
  hosts: all
  become: yes
  tasks:
    - name: Collect container facts
      community.general.docker_container_info:
        name: all
      register: container_info

    - name: Display container status
      debug:
        var: container_info.containers