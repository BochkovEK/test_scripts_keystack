✅ В чате - общаемся на русском
✅ В коде - комментарии только на английском
✅ Код пишем только после подтверждения

Критерии проверки контейнеров:
✅ OK (success):
    STATUS = "running"
    HEALTH = "healthy" (если есть)
    UPTIME > 1 минуты

⚠️ WARNING (предупреждение):
    STATUS = "running"
    HEALTH = "healthy"
    UPTIME < 1 минуты (недавно перезапущен)

❌ ERROR (ошибка):
    STATUS = "exited", "unhealthy", "restarting"

community.general.podman_container
yaml
- name: Get container info
  community.general.podman_container:
    name: all
    executable: podman
    state: info
  register: container_info
2. community.general.podman_container_info
yaml
- name: Get container information
  community.general.podman_container_info:
    name: all
  register: container_info
3. containers.podman.podman_container
yaml
- name: Inspect containers
  containers.podman.podman_container:
    name: "{{ item }}"
    state: inspected
  loop: "{{ container_list }}"
  register: container_inspect

Создать скрипт диагностики базовых сервисов Openstack
1) состояние контейнеров на узлах
2) keystone
3) mariadb (galera)
4) rabbitmq

openstack-diag/
├── ansible/
│   ├── ansible.cfg             # настройки Ansible: timeout, forks, gather_facts
│   └── playbooks/
│       └── containers.yml      # плейбуки диагностики
├── src/
│   ├── __init__.py
│   ├── config.py               # загрузка и управление конфигурацией
│   ├── default_config.py       # системные настройки (архитектура проекта)
│   ├── logger.py               # настройка логирования
│   └── ansible_runner.py       # запуск Ansible плейбуков
├── reports/                    # генерируемые отчёты (создаётся автоматически)
│
├── inventory.ini.template      # шаблон инвентори
├── inventory.ini               # пользовательский инвентори (в .gitignore)
├── config.yaml                 # пользовательский конфиг: paths, nodes, services, thresholds
├── diagnose.py                 # основная точка входа
├── test_config.py              # тесты конфигурации
├── README.md                   # документация
├── .gitignore                  # inventory.ini, reports/, логи
└── requirements.txt            # зависимости Python

pip install ansible-runner
pip install ansible

logger.py

Основной логгер - для diagnose.py, config.py
- Консоль: INFO
- Файл: DEBUG (diagnostics.log)

Ansible логгер - для ansible_runner.py
- Консоль: DEBUG
- Файл: DEBUG (ansible.log)

Report логгер - для отчетов
- Файл: INFO (reports.log)