---
- name: Check Ansible configuration using ansible-config
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Run ansible-config dump
      shell: ansible-config dump --only-changed
      register: config_dump

    - name: Show effective Ansible configuration
      debug:
        var: config_dump.stdout